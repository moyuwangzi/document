```sh
sudo zypper in pandoc
npm install textlint
npm install markdownlint
pip install grip pyright
sudo zypper in scrot
sudo zypper in graphviz
sudo zypper in graphviz python310-pygraphiviz
latex安装需要的外部插件
LaTeX主体部分 sudo zypper in texlive
lsp
sudo zypper in texlab
openSUSE比较特殊需要这么安装ripgrep
cargo install --features pcre2 ripgrep


sudo zypper install librime-devel emacs-el
sudo zypper in sqlite3
sudo zypper in libtool
```
